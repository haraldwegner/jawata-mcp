One file, in its own tree, because that is the only way this fixture can fail.

Placed beside a package-declaring sibling, the root derived from that sibling
covers this directory and the assertion passes whether or not the tab is
handled — which is what happened on the first attempt.
