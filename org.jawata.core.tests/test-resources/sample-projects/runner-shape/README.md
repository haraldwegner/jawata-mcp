The build/testrunner shape: production code under src/main/java that imports
JUnit because it RUNS tests. Content alone would mislabel it test code; the
folder convention (rule 2) must win first. No pom — conventions only.
