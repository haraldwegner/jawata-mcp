package com.example.leaf.c;

/** Self-contained: zero cross-boundary connascence, so it sits in the corpus's floor. */
public final class GammaLeaf {
    public long doubled(long value) {
        return value * 2;
    }
}
