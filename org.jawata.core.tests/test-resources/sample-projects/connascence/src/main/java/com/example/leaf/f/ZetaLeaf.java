package com.example.leaf.f;

/** Self-contained: zero cross-boundary connascence, so it sits in the corpus's floor. */
public final class ZetaLeaf {
    public boolean positive(long value) {
        return value > 0;
    }
}
