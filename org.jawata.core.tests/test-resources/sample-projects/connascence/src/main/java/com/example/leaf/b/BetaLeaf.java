package com.example.leaf.b;

/** Self-contained: zero cross-boundary connascence, so it sits in the corpus's floor. */
public final class BetaLeaf {
    public boolean blank(String value) {
        return value.isBlank();
    }
}
