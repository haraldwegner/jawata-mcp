package com.example.leaf.a;

/** Self-contained: zero cross-boundary connascence, so it sits in the corpus's floor. */
public final class AlphaLeaf {
    public int size(String value) {
        return value.length();
    }
}
