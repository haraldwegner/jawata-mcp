package com.example.leaf.d;

/** Self-contained: zero cross-boundary connascence, so it sits in the corpus's floor. */
public final class DeltaLeaf {
    public String trimmed(String value) {
        return value.trim();
    }
}
