package com.example.leaf.e;

/** Self-contained: zero cross-boundary connascence, so it sits in the corpus's floor. */
public final class EpsilonLeaf {
    public String upper(String value) {
        return value.toUpperCase();
    }
}
