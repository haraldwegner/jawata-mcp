package com.example;

/** Declared TEST code (pom testSourceDirectory) — deliberately importing no
 *  test framework, so only the declaration can classify it. */
public class ProdCheck {
    /** Returns a marker. */
    public String marker() { return "check"; }
}
