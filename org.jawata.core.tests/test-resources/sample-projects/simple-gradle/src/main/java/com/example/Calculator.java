package com.example;

/** Production class in a Gradle project — the target of the load test's semantic query. */
public class Calculator {

    /** Returns the sum of two operands. */
    public int add(int a, int b) {
        return a + b;
    }
}
