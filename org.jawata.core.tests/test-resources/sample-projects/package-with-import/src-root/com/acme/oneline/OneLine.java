package com.acme.oneline; import java.util.List;

/** Declaration and import share a line — legal, and once mis-read. */
public class OneLine {
    /** Returns an empty list. */
    public List<String> items() { return List.of(); }
}
