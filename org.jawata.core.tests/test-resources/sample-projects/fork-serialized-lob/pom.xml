<?xml version="1.0" encoding="UTF-8"?>
<!--
  Sprint 28d-rescue Stage 3 — a VERBATIM slice of the pinned upstream fork, for C3's
  clause that every row must be demonstrated on code we did not author.

  UNLIKE the other fork fixtures, four of this slice's eight files use Lombok, and JDT
  does not run Lombok's annotation processor here. That is recorded rather than hidden.

  It is vendored anyway because the alternative was no demonstration at all. Measured
  across the ENTIRE fork — 116 Lombok-free files plus 1210 Lombok-using ones — row 60's
  shape occurs in exactly one file: App.java here, which is itself one of the four that
  do NOT use Lombok. The operation applies to it and the pipeline reports the change
  compile-verified.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.jawata.fixtures</groupId>
    <artifactId>fork-serialized-lob</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>
    <dependencies>
        <dependency><groupId>org.projectlombok</groupId><artifactId>lombok</artifactId><version>1.18.36</version></dependency>
        <dependency><groupId>org.slf4j</groupId><artifactId>slf4j-api</artifactId><version>2.0.18</version></dependency>
        <dependency><groupId>com.h2database</groupId><artifactId>h2</artifactId><version>2.2.224</version></dependency>
    </dependencies>
</project>
