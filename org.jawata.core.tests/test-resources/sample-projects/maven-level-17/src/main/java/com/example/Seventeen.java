package com.example;

/** The single type of the {@code maven-level-17} fixture — enough for the
 *  project to be a real Java project with a real source root. */
public class Seventeen {

    /** Returns the level this project declares. */
    public String level() {
        return "17";
    }
}
