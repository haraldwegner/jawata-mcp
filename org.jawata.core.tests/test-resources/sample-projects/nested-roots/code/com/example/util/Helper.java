/** Declares NO package — the default package. Deriving a root from this file
 *  alone gives the directory it sits in, which is INSIDE the root its sibling
 *  produced. Only one of the two may be mounted. */
public class Helper {
    /** Returns a marker. */
    public String marker() { return "helper"; }
}
