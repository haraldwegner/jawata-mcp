package com.example;

/**
 * Names the collapsed level as a DECLARED TYPE, so the reference sweep has a file to repoint
 * that is neither the parent's nor a subtype's.
 */
public class TierDesk {

    public int bandOf(TierBanded banded) {
        return banded.band();
    }
}
