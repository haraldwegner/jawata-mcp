package com.example;

/** Exists only to give {@link ExtendedReuse} a subclass, which is what row 57 refuses on. */
public class ExtendedReuseChild extends ExtendedReuse {

    public ExtendedReuseChild(String book) {
        super(book);
    }
}
