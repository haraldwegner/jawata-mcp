package com.example;

/** Declares a two-segment package while sitting at the project root, so the
 *  derivation is asked for two parents it does not have. */
public class Widget {
    /** Returns a marker. */
    public String marker() { return "widget"; }
}
