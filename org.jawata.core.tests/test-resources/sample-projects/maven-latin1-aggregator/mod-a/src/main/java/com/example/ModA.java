package com.example;

/** The module whose sources are never mounted when the aggregator pom cannot be read. */
public class ModA {
    /** Returns a marker. */
    public String marker() { return "mod-a"; }
}
