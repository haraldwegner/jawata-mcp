=== Zeroed.java ===
package com.example;

/**
 * ROW 56's PERFORMING CASE — it exists to vary ONE thing, and declares no state of its own.
 */
public class Zeroed extends Levying {

    @Override
    public String rate() {
        return "zero";
    }
}
=== ZeroedBehaviour.java ===
package com.example;

/**
 * The behaviour Zeroed added to Levying, as an object.
 *
 * <p>Generated by Replace Subclass with Delegate. Zeroed still exists and
 * still extends Levying: adding the field, routing through it and deleting the
 * subclass are three steps that must happen together, and where the seam goes is
 * your decision rather than this row's.</p>
 */
public class ZeroedBehaviour {

    public String rate() {
        return "zero";
    }
}
